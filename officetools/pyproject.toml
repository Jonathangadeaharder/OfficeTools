[project]
name = "officetools"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
officetools = "officetools.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["officetools"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pyright>=1.1.409",
]
