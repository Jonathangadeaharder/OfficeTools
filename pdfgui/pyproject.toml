[project]
name = "pdfgui"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
pdfgui = "pdfgui.gui:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
