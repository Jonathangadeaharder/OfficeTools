[project]
name = "docgui"
version = "0.2.0"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
docgui = "docgui.launch:main"
docgui-gui = "docgui.gui:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pyright>=1.1.409",
]
