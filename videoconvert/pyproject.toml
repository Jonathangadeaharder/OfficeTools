[project]
name = "videoconvert"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
videoconvert = "videoconvert.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pyright>=1.1.409",
]
