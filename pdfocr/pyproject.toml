[project]
name = "pdfocr"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["ocrmypdf"]

[project.scripts]
pdfocr = "pdfocr.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pyright>=1.1.409",
]
