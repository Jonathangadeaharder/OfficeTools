[project]
name = "mp4audio"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
mp4audio = "mp4audio.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
